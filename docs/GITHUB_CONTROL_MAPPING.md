# GitHub Control Mapping

The mapping begins with a focused subset of GitHub-observable EMS controls. It is intentionally conservative. An unavailable API or insufficient permission must not be converted into PASS; use `NOT_EVALUATED`.

Later passes can add AWS, Azure, CI/CD, security scanner and EMS-governance collectors using the same normalized observation contract.
